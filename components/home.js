import * as React from 'react';
import { View, Text } from 'react-native';

export const Home = ({ navigation, route }) => {
    return (
        <View>
            <Text>Home page</Text>
        </View>
    );
}

