import * as React from 'react';
import { View, Text } from 'react-native';

export const Screen2 = ({ navigation, route }) => {
    return (
        <View>
            <Text>Screen2</Text>
        </View>
    );
}

